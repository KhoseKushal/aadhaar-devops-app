apiVersion: apps/v1
kind: Deployment
metadata:
  name: aadhaar-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aadhaar-app
  template:
    metadata:
      labels:
        app: aadhaar-app
    spec:
      containers:
      - name: aadhaar-app
        image: aadhaaracr.azurecr.io/aadhaar-app:530dd355527b57fb3442f7a6e397a4ccda6daaa9
        ports:
        - containerPort: 3000
        env:
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: db-config
              key: DB_HOST

